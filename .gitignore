*.dll
*.dylib
*.so
*.h
# built shared libraries (root dist/ only)
/dist/
.gomodcache/
.gocache/
cpa-key-policy-state.json
coverage.out

# CPA reference checkout used for research, not part of this project
_cpa_ref/
# Official panel reference checkout used for research, not part of this project
_panel_ref/

# web frontend build output (rebuilt by `make web-build`)
web/node_modules/
web/dist/
# tsc -b composite project artifacts
web/*.tsbuildinfo
web/vite.config.d.ts
web/vite.config.js
web/vitest.config.d.ts
web/vitest.config.js

# NOTE: internal/plugin/web/dist/index.html IS tracked. It is the go:embed
# source for the hosted UI. `make web-build` overwrites it with the real build;
# the committed copy is a placeholder so the Go build never fails.

node_modules/

# Local-only patch for pi-mcp-adapter SDK (Stitch MCP ajv fix). Not part
# of this plugin; kept for the maintainer's local pi setup.
scripts/patch-pi-mcp-sdk.mjs
